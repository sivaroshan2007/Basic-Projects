class Solution {
    public boolean isPalindrome(int x) {
        String num_str = String(x)
        Srting reversed_str = "";
        int i = -1
        while(i>=-(num_str.length())){
            reversed_str = reversed_str + num_str.charAt(-1)
        }
        
    }
}